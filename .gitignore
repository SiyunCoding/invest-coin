# API keys / secrets
.env
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs
venv/
.venv/
env/

# Cached market data — 루트 data/ 디렉토리의 내용물만 제외하고 paper_state.json만 whitelist.
# (data/ 자체를 ignore하면 negation 못 함. 또 src/data 같은 코드 디렉토리 보호 위해 루트 앵커)
/data/*
!/data/paper_state.json
*.parquet
*.csv

# Logs
*.log
logs/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Streamlit
.streamlit/secrets.toml

# Claude Code personal settings (project-level settings.json은 commit 가능, .local은 개인용)
.claude/settings.local.json
